---
version: 2

jobs:
  build:
    branches:
      only:
        - master
    docker:
      - image: majavapaja/majavashakki-ci
      - image: mongo:3.2.21-jessie
    steps:
      - checkout
      - run: ./.circleci/circleci.sh
